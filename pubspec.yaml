name: gestor_alumnos
description: Aplicación para gestionar alumnos y calificaciones (Unidad 3)
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2

  # Necesario para Material 3 y diseño moderno
  flutter_material_color_picker: ^1.1.0  # (Opcional, si luego quieres colores personalizados)

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/image/

  
